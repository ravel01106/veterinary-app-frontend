import React from "react";
import { VisibilityContextProp } from "../interfaces/VisibilityContextProp";


const VisbilityContext = React.createContext({} as VisibilityContextProp);

export default VisbilityContext;